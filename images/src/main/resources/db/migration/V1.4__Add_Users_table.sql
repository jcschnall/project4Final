create table CATS (
    ID serial,
    TITLE varchar(100) NOT NULL,
    IMAGE varchar(100) NOT NULL
);


create table TEXT (
    ID serial,
    TITLE varchar(100) NOT NULL,
    TEXT varchar(100) NOT NULL
);
